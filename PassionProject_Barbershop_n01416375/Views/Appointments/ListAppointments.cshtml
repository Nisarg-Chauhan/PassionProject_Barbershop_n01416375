@model IEnumerable<PassionProject_Barbershop_n01416375.Models.Appointments>

@{
    ViewBag.Title = "ListAppointments";
}

<h2>ListAppointments</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Barbers.bFirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Customers.firstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.appointmentDay)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Barbers.bFirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Customers.firstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.appointmentDay)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.appointmentId }) |
            @Html.ActionLink("Details", "Details", new { id=item.appointmentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.appointmentId })
        </td>
    </tr>
}

</table>
